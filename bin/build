#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail

declare BUILDPACK_TOP_DIR
BUILDPACK_TOP_DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd -P )
source "${BUILDPACK_TOP_DIR}/haskell-on-heroku.sh"

heroku_build "$@"
