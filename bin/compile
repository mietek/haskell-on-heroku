#!/usr/bin/env bash

set -eu

export BUILDPACK_TOP_DIR
BUILDPACK_TOP_DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd -P )

source "${BUILDPACK_TOP_DIR}/src.sh"

buildpack_compile "$1" "$2" "$3" || exit 1
